<!DOCTYPE html>
<html>
  <head>
    <title>Soccer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="h-screen">
    <header class="bg-gray-800 text-orange-50">
        <nav class="h-16 flex justify-between items-center w-[96%] mx-auto">
          <div>Stochastic Soccer</div>
          <div class="">
            <ul class="flex items-center gap-8">
              <li class="">
                <%= link_to "Standings", root_path, class: "hover:text-orange-300" %>
              </li>
              <li class="">
                <%= link_to "Matches", matches_path, class: "hover:text-orange-300" %>
              </li>
              <li class="">
                <%= link_to "Simulations", simulations_path, class: "hover:text-orange-300" %>
              </li>
            </ul>
          </div>
          <div class="">
            <buttom class="<%= button_class %>">Sign in</buttom>
          </div>
        </nav>
    </header>
    <main class="container mx-auto mt-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
